{
  "name": "fruit-catcher-game",
  "version": "1.9.5",
  "description": "A fun, mobile-friendly fruit catching game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build needed for web'",
    "postinstall": "node scripts/patch-capacitor-template.js && node scripts/patch-capacitor-agp9.js",
    "cap:sync": "npx cap sync",
    "cap:build": "npx cap build android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  }
}