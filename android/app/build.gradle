apply plugin: 'com.android.application'

def keystorePropertiesFile = rootProject.file("keystore.properties")
def keystoreProperties = new Properties()
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {
    namespace "com.fruitcatcher.game"
    compileSdk 36

    defaultConfig {
        applicationId "com.fruitcatcher.game"
        minSdk 24
        targetSdk 36
        versionCode 61
        versionName "1.9.4"
    }

    // Ship only the languages the app actually has resources for.
    // AGP 9 removed `resourceConfigurations`; use androidResources.localeFilters.
    androidResources {
        localeFilters += ["en"]
    }

    signingConfigs {
        release {
            if (keystorePropertiesFile.exists()) {
                storeFile file(keystoreProperties['storeFile'])
                storePassword keystoreProperties['storePassword']
                keyAlias keystoreProperties['keyAlias']
                keyPassword keystoreProperties['keyPassword']
            }
        }
    }

    buildTypes {
        release {
            // Keep R8 code shrinking and obfuscation enabled, but do not remove
            // runtime resources used by Capacitor and Unity Ads reflection.
            minifyEnabled true
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig keystorePropertiesFile.exists() ? signingConfigs.release : signingConfigs.debug
        }
        debug {
            signingConfig signingConfigs.debug
        }
    }

    // Per-device downloads instead of one universal APK inside the AAB.
    bundle {
        language { enableSplit = false }
        density  { enableSplit = true }
        abi      { enableSplit = true }
    }

    packaging {
        resources {
            excludes += [
                'META-INF/LICENSE*',
                'META-INF/NOTICE*',
                'DebugProbesKt.bin'
            ]
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    lint {
        abortOnError false
    }
}

repositories {
    google()
    mavenCentral()
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.activity:activity:$androidxActivityVersion"
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core:$androidxCoreVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    implementation project(':ad-id-compat')
    implementation 'com.unity3d.ads:unity-ads:4.12.1'
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
}

apply from: "capacitor.build.gradle"

// -- Web assets safety net ---------------------------------------------------
// Capacitor copies public/ into app/src/main/assets/public during `cap sync`.
// If a build runs without that step, mirror public/ into a generated assets
// dir so the APK/AAB always contains the game.
def webSourceDir = rootProject.file("../public")
def syncedAssets = file("src/main/assets/public/index.html")

if (webSourceDir.exists() && !syncedAssets.exists()) {
    def generatedAssetsDir = layout.buildDirectory.dir("generatedWebAssets")

    def syncWebAssets = tasks.register("syncWebAssets", Copy) {
        from webSourceDir
        into generatedAssetsDir.map { it.dir("public") }
    }

    android {
        sourceSets {
            main {
                assets.srcDirs += [generatedAssetsDir.get().asFile]
            }
        }
    }

    tasks.withType(com.android.build.gradle.tasks.MergeSourceSetFolders).configureEach {
        dependsOn syncWebAssets
    }
}