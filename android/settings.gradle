// Capacitor / Android project settings.
// AD_ID is declared directly in app/src/main/AndroidManifest.xml with the
// low-priority declaration in :ad-id-compat, so no settings-phase manifest
// patching is required.

include ':app'
include ':ad-id-compat'
project(':ad-id-compat').projectDir = new File('./ad-id-compat/')
include ':capacitor-cordova-android-plugins'
project(':capacitor-cordova-android-plugins').projectDir = new File('./capacitor-cordova-android-plugins/')

apply from: 'capacitor.settings.gradle'
